import {createStore,applyMiddleWare} from "redux";

import {thunk } from "redux-thunk";

const initialState={};

const rootReducer={};

const store = createStore(rootReducer,ini);

export default store;